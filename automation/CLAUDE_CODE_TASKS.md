# Encargo para Claude Code - pablocs116.github.io
Generado: 2026-07-07

## Contexto
`index.html` es un bundle empaquetado (ver `<script type="__bundler/manifest">` y `<script type="__bundler/template">`). Antes de tocar nada, revisa el commit d36baaf (https://github.com/pablocs116/pablocs116.github.io/commit/d36baaf) como ejemplo de cómo se añadió una entrada anteriormente. Prueba localmente (abre index.html o corre Jekyll) antes de hacer push.

## Aplicar directo (ya revisado, sin necesidad de preguntar a Pablo)
- [ ] PR mergeada `TransformerLensOrg/TransformerLens#1483` ("Add RecurrentGemma (Griffin) architecture adapter"): añadir a la sección "Open Source Contributions" (la sección ya existe en el sitio, junto a la entrada de la PR #1478). Motivo por el que califica: PR sustancial y no trivial (+264 líneas, 4 commits, cierra issue #1472, mergeada por el maintainer jlarson4). Datos a incluir: repo TransformerLensOrg/TransformerLens, título "Add RecurrentGemma (Griffin) architecture adapter", link https://github.com/TransformerLensOrg/TransformerLens/pull/1483, descripción breve: adapter de arquitectura RecurrentGemma/Griffin (RG-LRU + atención local), fecha de merge reciente (hace unos días).

## Necesita confirmación de Pablo antes de aplicar
(Ninguno pendiente por ahora. `TensorTonic-Solutions` fue rechazado por Pablo — no mostrar en el sitio, no volver a proponerlo.)

## Ya aplicado anteriormente (para referencia, no volver a tocar)
- Repo `Pilot_Experiments_Neuronpedia` — ya añadido a "SELECTED WORK" (confirmado en el sitio en vivo).
- PR `TransformerLensOrg/TransformerLens#1478` — ya añadida a "Open Source Contributions" (confirmado en el sitio en vivo).

## Al terminar
Actualiza `automation/state.json` marcando "applied" la PR #1483 una vez esté en el sitio.
